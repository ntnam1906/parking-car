<!DOCTYPE html>
<html>
    <head>
        <%- include('head'); -%>
		<link rel="stylesheet" href="/static/css/register.css" />
	</head>
    <body>
        <header>
            <a href="/"><img src="/static/images/logo.png" class="logo" alt=""></a>
        </header>
        <% if(error) {%>
            <div class="alert alert-danger"><%=error%></div>
        <%}%>
        <% if(error === null) {%>
            <div class="alert alert-success"><%=message%>.       Vui lòng <a href="/login" class="login-success">Đăng nhập</a></div>
        <%}%>
        <div class="content">
            <form role="form" method="post">
                <fieldset>
                    <div class="form-group">
                        <label for="text" class="user-text">Họ tên</label>
                        <input
                            class="form-control"
                            name="full_name"
                            type="text"
                            autofocus
                        />
                    </div>
                    <div class="form-group">
                        <label for="text" class="user-name">Username</label>
                        <input
                            class="form-control"
                            name="username"
                            type="text"
                        />
                    </div>
                    <div class="form-group">
                        <label for="text" class="user-pass">Password</label>
                        <input
                            class="form-control"
                            name="pass"
                            type="password"
                        />
                    </div>
                    <div class="form-group">
                        <label for="text" class="user-re-pass">Nhập lại Password</label>
                        <input
                            class="form-control"
                            name="re_pass"
                            type="password"
                        />
                    </div>
                    <button type="submit" class="btn btn-primary">
                        Đăng ký
                    </button>
                </fieldset>
            </form>
        </div>
    </body>
</html>