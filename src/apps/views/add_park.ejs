<!DOCTYPE html>
<html>
    <head>
        <%- include('head'); -%>
		<link rel="stylesheet" href="/static/css/edit_park.css" />
	</head>
    <body>
        <header class="header">
        </header>
        <a href="/parking-list" class="control">
            <img src="/static/images/back.png" alt="">
            Trở lại
        </a>
        <div class="control">
            <img src="/static/images/home.png" alt="" class="mb-2">
            Home
            <span class="symbol">/</span>
            <span class="desc">Quản lý bãi xe</span>
            <span class="symbol">/</span>
            <span class="desc">Thêm bãi xe mới</span>
        </div>
        <div class="title">Thêm bãi xe mới</div>
        <% if(error) {%>
            <div class="alert alert-danger"><%=error%></div>
        <%}%>
        
        <form method="post" class="form-add">
            <fieldset>
                <div class="form-group1">
                    <label for="text" class="user-text">Tên bãi xe</label>
                    <input
                        required
                        class="form-control1"
                        name="name"
                        type="text"
                    />
                </div>
                <div class="form-group1">
                    <label for="text" class="user-text">Tên vị trí</label>
                    <input
                        required
                        class="form-control1"
                        name="address"
                        type="text"
                    />
                </div>
                
                <button type="submit" class="btn btn-add">
                    Thêm
                </button>
            </fieldset>
        </form>

        </div>
    </body>
    
